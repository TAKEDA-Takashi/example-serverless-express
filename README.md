# example-serverless-express

## exec local dev server

```bash
$ npm start
```

## invoke local

```bash
$ npx sls invoke local -f server -d '{"path":"/"}'
```

## deploy

```bash
$ npx sls deploy
```
